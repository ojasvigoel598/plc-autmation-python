# Runtime dependencies — pinned to the exact versions the test suite passes
# against (verified by the CI workflow).  Bump deliberately, run the full
# suite, then update these pins together.
numpy==2.4.4
matplotlib==3.10.9       # legacy CLI modules (main.py)
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
websockets==17.0.1
